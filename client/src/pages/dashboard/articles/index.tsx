import { ArticlesTable } from "@/components/dashboard/articles/article-table";
import React from "react";

type Props = {};

const ArticlesDashboard: React.FC<Props> = ({}) => {
	return <ArticlesTable />;
};

export default ArticlesDashboard;
